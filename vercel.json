{
  "buildCommand": "cd web && rm -rf node_modules .npmrc && npm config set registry https://registry.npmjs.org/ && npm install --verbose 2>&1 | tail -50 && cd .. && node build-frontend.js",
  "outputDirectory": "web/dist"
}
