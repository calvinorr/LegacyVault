{
  "buildCommand": "cd web && rm -rf node_modules .npmrc package-lock.json && npm config set registry https://registry.npmjs.org/ && npm install vite@5.4.19 --force && npm install && cd .. && node build-frontend.js",
  "outputDirectory": "web/dist"
}
