# .env.example
# Copy this file to .env and fill in the actual values. Never commit your .env to version control!
#
# 🔒 SECURITY REQUIREMENTS:
# - All values marked as REQUIRED must be set or the server will not start
# - Use strong, random values for secrets (minimum 32 characters)
# - Never use placeholder values in production
# - Use HTTPS URLs in production

# ======================
# Server Configuration
# ======================
PORT=3000
NODE_ENV=development

# REQUIRED: Frontend URL for OAuth redirects
# Development: http://localhost:5173
# Production: https://your-app.vercel.app
FRONTEND_URL=http://localhost:5173

# ======================
# Google OAuth (REQUIRED)
# ======================
# Create credentials at: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID
# 2. Add authorized redirect URI: http://localhost:3000/auth/google/callback
# 3. Copy Client ID and Secret here

GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret-here
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# ======================
# Session Secret (REQUIRED)
# ======================
# MUST be at least 32 characters long!
# Generate a strong random value:
#   - macOS/Linux: openssl rand -base64 32
#   - Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# ⚠️  NEVER use placeholder values like 'replace-me' or 'changeme'
SESSION_SECRET=GENERATE_A_STRONG_32_CHAR_SECRET_HERE_USING_COMMAND_ABOVE

# ======================
# MongoDB (REQUIRED)
# ======================
# Local development (with Docker):
MONGO_URI=mongodb://mongo:27017/household_vault

# MongoDB Atlas (Production):
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority&ssl=true

# ======================
# CORS Configuration
# ======================
# Comma-separated list of allowed origins
# Development: http://localhost:5173,http://localhost:3000
# Production: https://your-app.vercel.app
ALLOWED_ORIGIN=http://localhost:5173,http://localhost:3000